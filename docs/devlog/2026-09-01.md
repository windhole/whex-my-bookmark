## 21:00 〜 Chrome先行の個人用ブックマーク拡張 v1

### やったこと
- ADR-0001〜0003 を実装前に起票（MV3+Vite、storage.local、Markdown方言と先頭8 H1）
- Chrome MV3 拡張のスキャフォルド、`make chrome`、firefox/safari は stub
- 制限付き Markdown の parse / serialize / 指定エリア末尾へ追記
- ツールバーの色付き8スロット保存、contextMenus、`Alt+1`〜`4` のコマンド
- オプションで Markdown 編集とファイル import/export
- 単体テスト 13件と `dist/chrome` の本番ビルドまで通した
- Chrome への読み込み確認は権限が必要なため、利用者側で行う

### 詰まった点・気づき
- Chrome が自動で割り当てる suggested_key は4つまでなので、5〜8は shortcuts 画面で付ける
- ポップアップ HTML の `crossorigin` は拡張ページでは邪魔になり得るので、ビルド後に外している

### 関連ADR / コミット
- ADR-0001 / ADR-0002 / ADR-0003

## 21:46 〜 ワンタッチ保存を6スロットに変更

### やったこと
- ADR-0004 を起票し、ADR-0003 のスロット数だけを更新
- 保存先を6つに減らし、閲覧メニューでは `Default Slot` 配下にまとめた
- 7個目以降の H1 は `Default Slot` の外に出す（保存ショートカットは付けない）

### 関連ADR / コミット
- ADR-0004

## 21:53 〜 Default Slot を Markdown の H1 にする

### やったこと
- ADR-0005 を起票し、ADR-0004 を superseded にした
- ワンタッチ6枠を `# Default Slot` 直下の H2 にした。メニューは見出し階層そのもの
- `# Default Slot` が無い旧文書は、トップレベル H1 をすべてその下へ移す

### 関連ADR / コミット
- ADR-0005

## 22:15 〜 保存を inbox ローカル配列に単純化

### やったこと
- ADR-0006 を起票。0002 / 0004 / 0005 を superseded
- ツールバークリックだけで inbox に保存。ポップアップと6スロット、Markdown 編集 UI を外した
- export は inbox を `# inbox` に載せて import 済み Markdown と merge するだけにした

### 詰まった点・気づき
- MV3 の service worker では `localStorage` が使えないので、inbox も `chrome.storage.local`

### 関連ADR / コミット
- ADR-0006

## 22:49 〜 inbox の一括消去

### やったこと
- オプション画面に Clear inbox を追加。確認ダイアログのあと配列を空にする
- 消去後はメニューとツールバーバッジも件数 0 に合わせる

### 関連ADR / コミット
- (ADRなし、既存の inbox 配列を空にする操作のため)
